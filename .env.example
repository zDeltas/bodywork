# Configuration Google OAuth 2.0 et APIs
# Copiez ce fichier en .env et remplissez les valeurs

# ===== AUTHENTIFICATION =====

# Google OAuth Android Client ID (depuis Google Cloud Console)
# Format: XXXXXX.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your_android_client_id_here.apps.googleusercontent.com

# Auth API URL (backend distant pour l'authentification)
EXPO_PUBLIC_AUTH_API_URL=https://api.gainizi.com

# ===== FEEDBACK =====

# Feedback API URL (localhost en dev, distant en prod)
# En développement : http://localhost:8080
# En production : https://api.gainizi.com
EXPO_PUBLIC_FEEDBACK_API_URL=http://localhost:8080

# ===== DEPRECATED (pour compatibilité) =====

# Backend URL (ancienne variable, sera supprimée dans une future version)
EXPO_PUBLIC_BACKEND_URL=https://api.gainizi.com

# ===== NOTES =====

# 1. Ces variables peuvent aussi être configurées dans app.json sous extra.*
#    car expo-auth-session les lit depuis Constants.expoConfig.extra
#
# 2. Les URLs en développement (__DEV__ = true) utilisent automatiquement localhost
#    sauf si override explicite
#
# 3. Pour ajouter un nouveau service API, suivez le pattern :
#    - Ajoutez la variable : EXPO_PUBLIC_YOUR_SERVICE_API_URL
#    - Ajoutez dans Config (app/utils/config.ts)
#    - Créez le client (app/services/api/YourServiceApiClient.ts)
